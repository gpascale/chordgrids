var app = window.ChordGrids = (window.ChordGrids || {});

app.ProfileLayout = Marionette.Layout.extend({
    template: app.Templates.ProfileLayout,
    className: 'profileLayoutView',
    el: 'body',
    events: {
    },
    initialize: function() {

    }
});